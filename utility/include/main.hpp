#ifndef _MAIN_HPP
#define _MAIN_HPP

/*!
  This package defines a default Lemur main() which 
  calls the application's main function AppMain() and
  handles uncaught exceptions.
*/

extern int AppMain(int argc, char *argv[]);

#endif 
